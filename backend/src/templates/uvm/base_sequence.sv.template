// {{SEQUENCE_NAME}}.sv
// Base UVM Sequence for {{AGENT_NAME}}
// Generated by UVM Testbench Chatbot

`ifndef {{SEQUENCE_NAME_UPPER}}_SV
`define {{SEQUENCE_NAME_UPPER}}_SV

class {{SEQUENCE_NAME}} extends uvm_sequence #({{TRANSACTION_TYPE}});
  `uvm_object_utils({{SEQUENCE_NAME}})

  // Sequence configuration
  rand int num_transactions;

  // Constraints
  constraint num_trans_c {
    num_transactions inside {[1:10]};
  }

  // Constructor
  function new(string name = "{{SEQUENCE_NAME}}");
    super.new(name);
  endfunction : new

  // Main sequence body
  virtual task body();
    `uvm_info(get_type_name(), $sformatf("Starting sequence with %0d transactions", num_transactions), UVM_MEDIUM)
    
    for (int i = 0; i < num_transactions; i++) begin
      `uvm_do(req)
    end
    
    `uvm_info(get_type_name(), "Sequence completed", UVM_MEDIUM)
  endtask : body

endclass : {{SEQUENCE_NAME}}

`endif // {{SEQUENCE_NAME_UPPER}}_SV
