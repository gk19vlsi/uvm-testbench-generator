// {{SEQUENCE_NAME}}.sv
// Directed UVM Sequence for {{SCENARIO_NAME}}
// Generated by UVM Testbench Chatbot

`ifndef {{SEQUENCE_NAME_UPPER}}_SV
`define {{SEQUENCE_NAME_UPPER}}_SV

class {{SEQUENCE_NAME}} extends {{BASE_SEQUENCE}};
  `uvm_object_utils({{SEQUENCE_NAME}})

  // Constructor
  function new(string name = "{{SEQUENCE_NAME}}");
    super.new(name);
  endfunction : new

  // Directed sequence body
  virtual task body();
    `uvm_info(get_type_name(), "Starting directed sequence: {{SCENARIO_NAME}}", UVM_MEDIUM)
    
    {{DIRECTED_LOGIC}}
    
    `uvm_info(get_type_name(), "Directed sequence completed", UVM_MEDIUM)
  endtask : body

endclass : {{SEQUENCE_NAME}}

`endif // {{SEQUENCE_NAME_UPPER}}_SV
