// {{INTERFACE_NAME}}.sv
// SystemVerilog Interface for {{AGENT_NAME}}
// Generated by UVM Testbench Chatbot

`ifndef {{INTERFACE_NAME_UPPER}}_SV
`define {{INTERFACE_NAME_UPPER}}_SV

interface {{INTERFACE_NAME}} (
  input logic {{CLOCK_SIGNAL}}{{RESET_SIGNALS}}
);

  // Interface signals
  {{SIGNAL_DECLARATIONS}}

  // Driver clocking block
  clocking driver_cb @(posedge {{CLOCK_SIGNAL}});
    default input #1step output #1;
    {{DRIVER_SIGNALS}}
  endclocking : driver_cb

  // Monitor clocking block
  clocking monitor_cb @(posedge {{CLOCK_SIGNAL}});
    default input #1step;
    {{MONITOR_SIGNALS}}
  endclocking : monitor_cb

  // Modport for driver
  modport driver_mp (
    clocking driver_cb,
    input {{CLOCK_SIGNAL}}{{RESET_SIGNALS}}
  );

  // Modport for monitor
  modport monitor_mp (
    clocking monitor_cb,
    input {{CLOCK_SIGNAL}}{{RESET_SIGNALS}}
  );

endinterface : {{INTERFACE_NAME}}

`endif // {{INTERFACE_NAME_UPPER}}_SV
