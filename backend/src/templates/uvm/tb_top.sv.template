// tb_top.sv
// Testbench Top Module for {{DUT_NAME}}
// Generated by UVM Testbench Chatbot

`timescale 1ns/1ps

module tb_top;

  // Import UVM package
  import uvm_pkg::*;
  `include "uvm_macros.svh"

  // Import testbench package
  import {{TB_PACKAGE}}::*;

  // Clock and reset signals
  {{CLOCK_DECLARATIONS}}
  {{RESET_DECLARATIONS}}

  // Interface instantiations
  {{INTERFACE_INSTANTIATIONS}}

  // DUT instantiation
  {{DUT_NAME}} dut (
    {{DUT_PORT_CONNECTIONS}}
  );

  // Clock generation
  {{CLOCK_GENERATION}}

  // Reset generation
  initial begin
    {{RESET_GENERATION}}
  end

  // Set virtual interfaces in config_db
  initial begin
    {{CONFIG_DB_SETS}}
  end

  // Start test
  initial begin
    // Enable waveform dumping
    $dumpfile("{{DUT_NAME}}.vcd");
    $dumpvars(0, tb_top);
    
    // Run test
    run_test();
  end

  // Watchdog timer
  initial begin
    #{{TIMEOUT}};
    `uvm_fatal("TIMEOUT", "Test timeout - simulation ran too long")
  end

endmodule : tb_top
