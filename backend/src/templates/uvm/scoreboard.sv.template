// {{COMPONENT_NAME}}.sv
// UVM Scoreboard for {{DUT_NAME}}
// Generated by UVM Testbench Chatbot

`ifndef {{COMPONENT_NAME_UPPER}}_SV
`define {{COMPONENT_NAME_UPPER}}_SV

class {{COMPONENT_NAME}} extends uvm_scoreboard;
  `uvm_component_utils({{COMPONENT_NAME}})

  // Analysis imports for receiving transactions
  {{ANALYSIS_IMPORTS}}

  // Transaction queues
  {{TRANSACTION_QUEUES}}

  // Scoreboard statistics
  int transactions_compared = 0;
  int transactions_matched = 0;
  int transactions_mismatched = 0;

  {{TIMING_TOLERANCE}}

  {{ERROR_LOGGING}}

  // Constructor
  function new(string name = "{{COMPONENT_NAME}}", uvm_component parent = null);
    super.new(name, parent);
    {{ANALYSIS_IMPORT_CREATES}}
  endfunction : new

  // Build phase
  virtual function void build_phase(uvm_phase phase);
    super.build_phase(phase);
  endfunction : build_phase

  // Connect phase
  virtual function void connect_phase(uvm_phase phase);
    super.connect_phase(phase);
  endfunction : connect_phase

  // Write functions for analysis imports
  {{WRITE_FUNCTIONS}}

  // Comparison logic
  {{COMPARISON_LOGIC}}

endclass : {{COMPONENT_NAME}}

`endif // {{COMPONENT_NAME_UPPER}}_SV
