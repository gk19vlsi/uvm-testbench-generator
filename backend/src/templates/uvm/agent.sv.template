// {{COMPONENT_NAME}}.sv
// UVM Agent for {{AGENT_NAME}}
// Generated by UVM Testbench Chatbot

`ifndef {{COMPONENT_NAME_UPPER}}_SV
`define {{COMPONENT_NAME_UPPER}}_SV

class {{COMPONENT_NAME}} extends uvm_agent;
  `uvm_component_utils({{COMPONENT_NAME}})

  // Agent components
  {{DRIVER_DECLARATION}}
  {{MONITOR_DECLARATION}}
  {{SEQUENCER_DECLARATION}}

  // Agent configuration
  bit is_active = {{IS_ACTIVE}};

  // Constructor
  function new(string name = "{{COMPONENT_NAME}}", uvm_component parent = null);
    super.new(name, parent);
  endfunction : new

  // Build phase - create agent components
  virtual function void build_phase(uvm_phase phase);
    super.build_phase(phase);

    // Create monitor (always present)
    {{MONITOR_CREATE}}

    // Create driver and sequencer only for active agents
    if (is_active == UVM_ACTIVE) begin
      {{DRIVER_CREATE}}
      {{SEQUENCER_CREATE}}
    end
  endfunction : build_phase

  // Connect phase - connect driver to sequencer
  virtual function void connect_phase(uvm_phase phase);
    super.connect_phase(phase);

    if (is_active == UVM_ACTIVE) begin
      {{DRIVER_SEQUENCER_CONNECT}}
    end
  endfunction : connect_phase

endclass : {{COMPONENT_NAME}}

`endif // {{COMPONENT_NAME_UPPER}}_SV
