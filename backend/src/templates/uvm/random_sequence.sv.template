// {{SEQUENCE_NAME}}.sv
// Random UVM Sequence with Constraints
// Generated by UVM Testbench Chatbot

`ifndef {{SEQUENCE_NAME_UPPER}}_SV
`define {{SEQUENCE_NAME_UPPER}}_SV

class {{SEQUENCE_NAME}} extends {{BASE_SEQUENCE}};
  `uvm_object_utils({{SEQUENCE_NAME}})

  // Constructor
  function new(string name = "{{SEQUENCE_NAME}}");
    super.new(name);
  endfunction : new

  // Random sequence body with constraints
  virtual task body();
    `uvm_info(get_type_name(), "Starting random sequence", UVM_MEDIUM)
    
    for (int i = 0; i < num_transactions; i++) begin
      `uvm_do_with(req, {
        {{RANDOM_CONSTRAINTS}}
      })
    end
    
    `uvm_info(get_type_name(), "Random sequence completed", UVM_MEDIUM)
  endtask : body

endclass : {{SEQUENCE_NAME}}

`endif // {{SEQUENCE_NAME_UPPER}}_SV
