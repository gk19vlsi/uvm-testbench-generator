// {{SEQUENCE_NAME}}.sv
// Error Injection UVM Sequence
// Generated by UVM Testbench Chatbot

`ifndef {{SEQUENCE_NAME_UPPER}}_SV
`define {{SEQUENCE_NAME_UPPER}}_SV

class {{SEQUENCE_NAME}} extends {{BASE_SEQUENCE}};
  `uvm_object_utils({{SEQUENCE_NAME}})

  // Constructor
  function new(string name = "{{SEQUENCE_NAME}}");
    super.new(name);
  endfunction : new

  // Error injection sequence body
  virtual task body();
    `uvm_info(get_type_name(), "Starting error injection sequence", UVM_MEDIUM)
    
    {{ERROR_INJECTION_LOGIC}}
    
    `uvm_info(get_type_name(), "Error injection sequence completed", UVM_MEDIUM)
  endtask : body

endclass : {{SEQUENCE_NAME}}

`endif // {{SEQUENCE_NAME_UPPER}}_SV
